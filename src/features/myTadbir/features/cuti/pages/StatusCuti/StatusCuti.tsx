import React, {FC} from 'react';
import {View} from 'react-native';
import {Text} from 'react-native-paper';

const StatusCuti: FC = () => (
  <View
    style={{
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
    }}>
    <Text>Status Cuti</Text>
  </View>
);

export default StatusCuti;
