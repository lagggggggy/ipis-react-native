import React, {FC} from 'react';
import {View} from 'react-native';
import {Text} from 'react-native-paper';

const PegawaiCuti: FC = () => (
  <View
    style={{
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
    }}>
    <Text>Pegawai Cuti</Text>
  </View>
);

export default PegawaiCuti;
